% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_songs_by_vibe.R
\name{filter_songs_by_vibe}
\alias{filter_songs_by_vibe}
\title{Filter Songs by Vibe}
\usage{
filter_songs_by_vibe(
  data,
  energy_range,
  valence_range,
  dance_range,
  genre = "All"
)
}
\arguments{
\item{data}{A data frame of songs with vibe features.}

\item{energy_range}{A numeric vector of length 2 (min, max).}

\item{valence_range}{A numeric vector of length 2 (min, max).}

\item{dance_range}{A numeric vector of length 2 (min, max).}

\item{genre}{Selected genre (or "All").}
}
\value{
A filtered data frame.
}
\description{
Filters a dataset of songs based on vibe-related slider input ranges and genre selection.
}
\examples{
# Load pipe and filter functions
library(dplyr)

# Create mock data
mock_data <- data.frame(
  song = c("Song A", "Song B", "Song C"),
  artist = c("Artist A", "Artist B", "Artist C"),
  genre = c("Pop", "Pop", "Rock"),
  energy = c(0.6, 0.9, 0.4),
  valence = c(0.7, 0.5, 0.2),
  danceability = c(0.8, 0.6, 0.3)
)

# Filter mock data
filtered <- filter_songs_by_vibe(mock_data, c(0.5, 1), c(0.4, 1), c(0.5, 1), "Pop")
print(filtered)
}
